# =============================================================================
# VECTOR MVP - Environment Variables Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Database Configuration (REQUIRED for production)
# -----------------------------------------------------------------------------
# Get a free MongoDB Atlas cluster at: https://www.mongodb.com/cloud/atlas
# Connection string format: mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/vector?retryWrites=true&w=majority"

# -----------------------------------------------------------------------------
# LLM Provider Configuration (At least ONE is REQUIRED)
# -----------------------------------------------------------------------------
# Configure at least one LLM provider. Vector will automatically use the first
# available provider if DEFAULT_LLM_PROVIDER is not set.

# OpenAI API Key
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-..."

# Anthropic (Claude) API Key
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# Google Gemini API Key
# Get yours at: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY="AIza..."

# -----------------------------------------------------------------------------
# LLM Provider Selection (OPTIONAL)
# -----------------------------------------------------------------------------
# Choose which LLM provider to use by default: "openai", "anthropic", or "google"
# If not set, Vector will use the first available provider in this order:
# Google → OpenAI → Anthropic
DEFAULT_LLM_PROVIDER="google"

# Optional: Override default models for each provider
DEFAULT_OPENAI_MODEL="gpt-4o-mini"           # or "gpt-4o", "gpt-4-turbo"
DEFAULT_ANTHROPIC_MODEL="claude-3-5-sonnet-latest"  # or "claude-3-opus-latest"
DEFAULT_GOOGLE_MODEL="gemini-2.0-flash"     # or "gemini-1.5-pro"

# -----------------------------------------------------------------------------
# NextAuth Configuration (REQUIRED for authentication)
# -----------------------------------------------------------------------------
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-minimum-32-characters"

# URL where your app is hosted
NEXTAUTH_URL="http://localhost:3000"              # Development
# NEXTAUTH_URL="https://vector.yourdomain.com"    # Production

# -----------------------------------------------------------------------------
# Demo/Development Mode (OPTIONAL)
# -----------------------------------------------------------------------------
# Force demo mode (uses mock data instead of real LLMs/DB)
# Set to "true" only for testing without API keys
# VECTOR_FORCE_DEMO="false"

# Use mock orchestrator even if LLM providers are configured
# VECTOR_USE_MOCK_ORCHESTRATOR="false"

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV="development"  # or "production"

# -----------------------------------------------------------------------------
# Public Environment Variables (accessible in browser)
# -----------------------------------------------------------------------------
# These should be prefixed with NEXT_PUBLIC_
NEXT_PUBLIC_DEMO_MODE="false"  # Shows demo badges in UI when "true"
